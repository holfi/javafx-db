create procedure getotchet(begin_date timestamp without time zone, end_date timestamp without time zone)
    language sql
as
$$

$$;

alter procedure getotchet(timestamp, timestamp) owner to postgres;

